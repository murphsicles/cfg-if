[package]
name = "@std/cfg-if"
version = "1.0.4"
description = "Conditional compilation macro for Zeta — cfg_if! { if #[cfg(...)] { ... } }"
license = "MIT"
repository = "https://github.com/murphsicles/cfg-if"
readme = "README.md"
